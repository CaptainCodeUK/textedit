@* Minimal placeholder component for alternative editor prototype. *@
<div id="alt-editor" class="alt-editor-placeholder text-monospace p-2 bg-light border" role="region">
  <div class="mb-2"><strong>AltEditor (prototype)</strong></div>
  <textarea class="w-100" rows="10" @bind="Value"></textarea>
  <div class="mt-2">
    <button class="btn btn-primary me-2" @onclick="Save">Save</button>
    <button class="btn btn-secondary" @onclick="Open">Open</button>
  </div>
</div>

@code {
    [Parameter]
    public string Value { get; set; } = string.Empty;

    [Parameter]
    public EventCallback<string> ValueChanged { get; set; }

    private void Save()
    {
        // TODO: wire to DocumentService in prototype
    }

    private void Open()
    {
        // TODO: select a document via DocumentService in prototype
    }
}
