@page "/"
@layout MainLayout
@using Microsoft.AspNetCore.Components

<div class="app-container">
    <h1>TextEdit</h1>
    <p>Text editor UI will be implemented in Phase 3 (User Story 1)</p>
    @* Editor component will be added here: <TextEditor /> *@
</div>

@code {
    // App state and lifecycle
    protected override void OnInitialized()
    {
        // Initialize app state
        // Load session persistence if available (Phase 5)
    }
}
