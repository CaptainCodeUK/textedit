@page "/"
@layout MainLayout
@using Microsoft.AspNetCore.Components

<div class="app-container">
    <TextEditor />
</div>

@code {
    // App state and lifecycle
    protected override void OnInitialized()
    {
        // Initialize app state
        // Load session persistence if available (Phase 5)
    }
}
