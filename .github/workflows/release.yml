name: Release Build

on:
  push:
    branches:
      - 'release/*'  # Trigger on release branches (e.g., release/v1.2.1)
  workflow_dispatch:  # Allow manual trigger for testing

# Permissions required to create releases and optionally open issues
permissions:
  contents: write
  issues: write

# Cancel in-progress builds when new push to same ref
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: Windows
            runner: windows-latest
            target: win
            artifact-pattern: '**/*.exe'
          - os: macOS
            runner: macos-latest
            target: osx
            artifact-pattern: '**/*.dmg'
          - os: Linux
            runner: ubuntu-latest
            target: linux
            artifact-pattern: '**/*.AppImage'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for version extraction
      

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Setup .NET 6 (for ElectronNET CLI)
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: Install libfuse2 (Linux AppImage requirement)
        if: matrix.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libfuse2
      
      - name: Install Electron.NET CLI
        run: dotnet tool install ElectronNET.CLI -g
      
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}

      
      - name: Restore dependencies
        run: dotnet restore textedit.sln
      
      - name: Build solution
        run: dotnet build textedit.sln --configuration Release --no-restore
      
      - name: Run tests
        run: dotnet test textedit.sln --configuration Release --no-build --verbosity normal
      
      - name: Extract version from ref (branch or tag)
        id: version
        shell: bash
        run: |
          if [[ "${{ github.ref }}" == refs/heads/release/* ]]; then
            VERSION=${GITHUB_REF#refs/heads/release/}
          elif [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/v}
          else
            VERSION="0.0.0-dev"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Generate electron-builder configuration for electronize
        shell: bash
        working-directory: src/TextEdit.App
        env:
          BUILD_VERSION: ${{ steps.version.outputs.version }}
        run: |
          echo "Generating electron-builder.json for version $BUILD_VERSION"
          # Ensure the helper is available and run it; it writes ./obj/Host/bin/electron-builder.json
          node ./obj/Host/build-helper.js electron.manifest.json $BUILD_VERSION || true
          # Electronize expects config under obj/desktop/<platform>/bin — copy for the current matrix target
          PLATFORM_DIR="./obj/desktop/${{ matrix.target }}/bin"
          mkdir -p "$PLATFORM_DIR"
          if [ -f ./obj/Host/bin/electron-builder.json ]; then
            cp ./obj/Host/bin/electron-builder.json "$PLATFORM_DIR/"
            echo "Copied electron-builder.json to $PLATFORM_DIR"
          else
            echo "Warning: obj/Host/bin/electron-builder.json not found; electronize may fail if configuration is required." >&2
          fi

      - name: Generate electron-builder configuration for electronize
        shell: bash
        working-directory: src/TextEdit.App
        env:
          BUILD_VERSION: ${{ steps.version.outputs.version }}
        run: |
          echo "Generating electron-builder.json for version $BUILD_VERSION"
          # Ensure the helper is available and run it; it writes ./obj/Host/bin/electron-builder.json
          node ./obj/Host/build-helper.js electron.manifest.json $BUILD_VERSION || true
          # Electronize expects config under obj/desktop/<platform>/bin — copy for the current matrix target
          PLATFORM_DIR="./obj/desktop/${{ matrix.target }}/bin"
          mkdir -p "$PLATFORM_DIR"
          if [ -f ./obj/Host/bin/electron-builder.json ]; then
            cp ./obj/Host/bin/electron-builder.json "$PLATFORM_DIR/"
            echo "Copied electron-builder.json to $PLATFORM_DIR/"
          else
            echo "Warning: obj/Host/bin/electron-builder.json not found; electronize may fail if configuration is required." >&2
          fi
      
      - name: Build Electron app for ${{ matrix.os }}
        working-directory: src/TextEdit.App
        env:
          DEBUG: electron-builder
        run: electronize build /target ${{ matrix.target }} /Release | tee electronize-build-${{ matrix.os }}.log

      - name: Upload build log
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ matrix.os }}-${{ steps.version.outputs.version }}
          path: src/TextEdit.App/electronize-build-${{ matrix.os }}.log
          retention-days: 7
      
      - name: Print electronize build log
        shell: bash
        run: |
          echo "=== electronize build log (${{ matrix.os }}) ==="
          if [ -f src/TextEdit.App/electronize-build-${{ matrix.os }}.log ]; then
            sed -n '1,400p' src/TextEdit.App/electronize-build-${{ matrix.os }}.log || true
          else
            echo "Build log not found at src/TextEdit.App/electronize-build-${{ matrix.os }}.log" >&2
          fi

      - name: List build output (debug)
        shell: bash
        run: |
          echo "=== Search common output roots (repo root) ==="
          for d in bin/Desktop src/TextEdit.App/bin/Desktop dist out; do
            if [ -d "$d" ]; then
              echo "-- $d --"; ls -R "$d" || true; echo "";
            fi
          done
          echo "=== Candidate installer files (repo scan) ==="
          find . -type f \( -name "*Setup*.exe" -o -name "*.nupkg" -o -name "RELEASES" -o -name "*.dmg" -o -name "*.zip" -o -name "*.AppImage" -o -name "*.deb" \) 2>/dev/null | sed 's#^./##' || true

      - name: Stage installable artifacts only
        shell: bash
        id: stage
        run: |
          mkdir -p installer-artifacts
          ROOT=.
          # Windows: NSIS Setup.exe + Squirrel artifacts (nupkg, RELEASES)
          if [ "${{ matrix.target }}" = "win" ]; then
            find "$ROOT" -type f -name "*Setup*.exe" ! -path "*/node_modules/*" -exec cp -v {} installer-artifacts/ \; 2>/dev/null || true
            find "$ROOT" -type f -name "*.nupkg" ! -path "*/node_modules/*" -exec cp -v {} installer-artifacts/ \; 2>/dev/null || true
            find "$ROOT" -type f -name "RELEASES" ! -path "*/node_modules/*" -exec cp -v {} installer-artifacts/ \; 2>/dev/null || true
          fi
          # macOS: dmg (fallback to zip if dmg missing)
          if [ "${{ matrix.target }}" = "osx" ]; then
            if find "$ROOT" -type f -name "*.dmg" | grep -q ".dmg"; then
              find "$ROOT" -type f -name "*.dmg" -exec cp -v {} installer-artifacts/ \; 2>/dev/null || true
            else
              find "$ROOT" -type f -name "*.zip" -exec cp -v {} installer-artifacts/ \; 2>/dev/null || true
            fi
          fi
          # Linux: AppImage (include deb if present)
          if [ "${{ matrix.target }}" = "linux" ]; then
            find "$ROOT" -type f -name "*.AppImage" -exec cp -v {} installer-artifacts/ \; 2>/dev/null || true
            find "$ROOT" -type f -name "*.deb" -exec cp -v {} installer-artifacts/ \; 2>/dev/null || true
          fi
          echo "Staged files:"; ls -1 installer-artifacts || true
          COUNT=$(ls -1 installer-artifacts 2>/dev/null | wc -l | xargs)
          if [ "$COUNT" = "0" ]; then
            echo "Error: No installable artifacts staged for target ${{ matrix.target }}" >&2
            echo "Hint: Check build log artifact (build-log-${{ matrix.os }}-${{ steps.version.outputs.version }}) and the printed log above for electronize output." >&2
            exit 1
          fi

      - name: Upload installable artifacts
        uses: actions/upload-artifact@v4
        with:
          name: textedit-${{ matrix.os }}-${{ steps.version.outputs.version }}
          path: installer-artifacts/
          retention-days: 30
  
  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/heads/release/')
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Extract version from ref (branch or tag)
        id: version
        shell: bash
        run: |
          if [[ "${{ github.ref }}" == refs/heads/release/* ]]; then
            VERSION=${GITHUB_REF#refs/heads/release/}
          elif [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/v}
          else
            echo "Unable to determine version from ref: $GITHUB_REF" >&2
            exit 1
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: textedit-*
          path: artifacts

      - name: List downloaded artifacts (debug)
        run: |
          echo "=== Downloaded artifacts ==="
          ls -R artifacts/ || echo "No artifacts directory"
          echo ""
          echo "=== Artifact structure ==="
          find artifacts -type f || echo "No files found"
      
      - name: Fail if no artifacts found
        run: |
          if [ ! -d artifacts ] || [ -z "$(find artifacts -type f 2>/dev/null)" ]; then
            echo "Error: No artifacts downloaded. Ensure build job uploaded artifacts with expected names." >&2
            echo "Expected artifacts matching pattern: textedit-*" >&2
            exit 1
          fi
          
          FILE_COUNT=$(find artifacts -type f | wc -l)
          echo "Downloaded $FILE_COUNT files across all artifacts"
      
      - name: Generate release notes
        id: release_notes
        run: |
          # Get previous tag
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
          
          if [ -z "$PREV_TAG" ]; then
            # First release - use all commits
            COMMITS=$(git log --pretty=format:"- %s (%h)" --no-merges)
          else
            # Get commits since previous tag
            COMMITS=$(git log $PREV_TAG..HEAD --pretty=format:"- %s (%h)" --no-merges)
          fi
          
          # Create release notes
          cat > release_notes.md << EOF
          ## What's New in v${{ steps.version.outputs.version }}
          
          ### Changes
          $COMMITS
          
          ### Installation
          
          - **Windows**: Download and run \`Scrappy Text Editor Setup ${{ steps.version.outputs.version }}.exe\`
          - **macOS**: Download and open \`Scrappy Text Editor-${{ steps.version.outputs.version }}.dmg\`
          - **Linux**: Download \`Scrappy Text Editor-${{ steps.version.outputs.version }}.AppImage\`, make executable, and run
          
          ### Auto-Update
          If you're already using TextEdit, the app will automatically notify you of this update.
          EOF
          
          cat release_notes.md
      
      - name: Create GitHub Release (no assets)
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}
          target_commitish: ${{ github.sha }}
          name: Release v${{ steps.version.outputs.version }}
          body_path: release_notes.md
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload release assets (overwrite)
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          tag: v${{ steps.version.outputs.version }}
          file: artifacts/**/*
          file_glob: true
          overwrite: true
      
      - name: Notify on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🔴 Release build failed for ${{ github.ref }}',
              body: 'Release build failed. Check the [workflow run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}) for details.',
              labels: ['build-failure', 'ci']
            })
