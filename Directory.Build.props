<Project>
  <PropertyGroup>
    <!-- Per-project coverage thresholds configured in individual test projects -->
    <!-- Global threshold removed to allow per-project control -->
  </PropertyGroup>
</Project>
