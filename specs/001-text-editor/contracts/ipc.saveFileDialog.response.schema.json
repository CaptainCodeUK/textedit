{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/textedit/ipc/saveFileDialog.response.schema.json",
  "title": "SaveFileDialogResponse",
  "type": "object",
  "properties": {
    "canceled": { 
      "type": "boolean",
      "description": "True if user canceled the dialog"
    },
    "filePath": {
      "type": ["string", "null"],
      "description": "Selected file path, or null if canceled"
    }
  },
  "required": ["canceled"],
  "additionalProperties": false
}
